/**
 * @deprecated
 * This standalone socket server is DEPRECATED and unused.
 * Socket.IO handling has been consolidated into the Next.js API route at `pages/api/socket.ts`.
 * Running Socket.IO inside Next.js avoids running a separate process/port and shares the same deployment lifecycle.
 */

export {};

